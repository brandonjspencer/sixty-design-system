/**
 * Design System Tokens
 * 
 * Main entry point for the design system tokens.
 * Build the project first: npm run build
 */

export { default as tokens } from './dist/tokens.js';

// Re-export token categories for convenience
export { default } from './dist/tokens.js';
